# 项目开发约定

## 版本规则

- 每个独立改动批次都必须递增扩展版本号。
- 用户未指定版本级别时，默认递增补丁版本。
- `manifest.json`、`package.json`、`package-lock.json`、发布测试和当前发布文档中的版本必须保持一致。

## Git 分支与浏览器手动测试

- 所有功能开发都直接在项目固定原路径 `/Users/psh/codes/video_notes` 中进行，不创建或使用 Git worktree；只有用户明确要求时才允许例外。
- 开始独立开发任务前，先检查当前分支和未提交改动；保留用户已有改动，并在原路径中新建或切换到对应的 `codex/` 功能分支。
- 不在不同工作目录之间复制源文件或同步 `dist`。构建、测试和打包都在项目原路径的当前功能分支中执行。
- Edge/Chrome 开发者模式始终加载固定目录 `/Users/psh/codes/video_notes/dist`。代码修改后先在原路径重新构建，再在扩展管理页点击“重新加载”，无需更换扩展目录或重启浏览器。
- 浏览器手动测试前，必须确认 `/Users/psh/codes/video_notes/dist/manifest.json` 的版本与当前功能版本一致，并确认最新源码已经构建到该目录。
- 浏览器扩展的实机重载和手动测试由用户执行。Codex 负责完成构建、产物检查并提供可复现的实机测试清单；除非用户在当次任务中明确要求，否则不得通过 UI 自动化直接操作 Edge、Chrome 或其中的网页。
