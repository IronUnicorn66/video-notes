# Edge 150/152 与 Chrome 138+ 实机验收清单（1.0.26）

此清单区分可自动执行的发布校验和需要在桌面 Edge、Chrome 中完成的人工验收。人工项目由用户实际操作并记录结果，未执行前保持“待用户手动验收”。

## 一、发布自动校验

- [x] 已自动验证：`npm test` 通过（327 个测试），`npm run build` 与 `npm run package` 均通过。
- [x] 已自动验证：`manifest.json`、`package.json`、`package-lock.json`、`dist/manifest.json` 和发布 ZIP 版本均为 `1.0.26`。
- [x] 已自动验证：`unzip -t artifacts/video-notes-edge-1.0.26.zip` 与 SHA-256 校验通过；根目录包含 `manifest.json`、`sidepanel.html` 和 `offscreen.html`。
- [x] 已自动验证：当前会话本地字幕按前置时间范围选择完整段落，500 字限制从最早段落开始收缩；范围全部翻译时保存独立译文，部分译文、旧会话、Bilibili 或本地来源缺失时保持安全回退。
- [x] 已自动验证：发布 ZIP 不包含 `ggml-small-q5_1.bin` 或 `ggml-medium-q5_0.bin`。
- [x] 已自动验证：`git diff --check` 无空白错误。
- [x] 已在实际 Chromium 窄宽度页面验证：“原文 / 译文”位于“重新翻译”和“重试”之间，三种显示模式正确，最后一个选项不能取消，未完整翻译时控件隐藏且原文和已有译文保持可见。
- [x] 已在隔离 Edge 152 运行时验证：固定 `dist` 的运行版本为 `1.0.24`，560px 窄侧栏中“翻译 / 定位 / 重试 / 字号 +/−”同排对齐，定位高亮样式可见；播放器停在 616 秒时读取结果为 616 秒，读取前后播放器进度均保持 616 秒。用户个人 Edge 中的完整点击流程仍保留在下方人工验收项。
- [x] 已在实际 Edge 中使用 320px 侧栏布局用例验证：中英文均按“标题、排序、弯箭头撤销/反撤销、独立 `+`、独立 `−`、清空、导出”的顺序保持单行；按钮按内容取宽，清空不伸展，两个字号按钮边框分离，工具栏及按钮文字均无横向溢出。笔记正文在 10/13/24px 时分别呈现预期字号，附带字幕保持至少 10px。Chrome 外部会话未连接，不计为 Chrome 实机已验证。

### 此前完成的 Edge 150 实机烟测

- [x] 用户在 Edge 150 加载 1.0.1 生产构建，确认扩展和视频笔记侧栏可以打开。
- [x] 用户在 YouTube 开启原生字幕后创建标记，确认卡片能读取标记前字幕。
- [x] 用户在 YouTube 开启沉浸式翻译双语字幕后创建标记，确认卡片保留双语字幕。

### 此前完成的 Edge 152 实机烟测

- [x] 在 Edge 152 重新加载 `dist`，确认 1.0.18 设置页与包含 1081 段英文字幕的完整字幕侧栏正常打开。
- [x] 实机确认字幕源语言只读显示为“英语”，目标可切换为“西班牙语”；自动化检查确认下拉框仅包含简体中文、英语、日语、韩语和西班牙语。
- [x] 实机确认语言包下载进度从 19% 更新到 55% 并完成；完成后显示“已下载 · 约 200 MiB”，本机语言包目录实际约 198 MiB，期间没有字幕翻译服务权限提示。

以下人工项目尚未全部执行，保持待验收状态。

## 二、模型下载与转写（待用户手动验收）

- [ ] 分别下载 Base · 57 MiB（59,707,625 字节）、Small · 181 MiB（190,085,487 字节）和 Medium · 514 MiB（539,212,467 字节）；确认它们都来自固定 Hugging Face revision `98aa99a0a9db05ae2342309f5096248665f7cba3`。
- [ ] 中断一次 Small 下载后继续下载，确认续传完成并通过校验。
- [ ] 重启 Edge 后确认三种模型仍显示为已下载。
- [ ] 使用同一条 3–15 秒中文及中英混合录音，分别以 Base、Small、Medium 重新转写；确认卡片和 Markdown 保留三条结果。
- [ ] 在编辑个人正文后重新转写，确认个人正文保持不变，新结果进入候选和历史。
- [ ] 下载、录音或转写进行时尝试切换模型，确认控件不可切换；确认同一时刻内存中只有一个 Whisper 实例。
- [ ] 在桌面 Edge 中使用 Medium，记录下载、加载和转写耗时及内存，确认机器可承受其较高资源消耗。

## 三、笔记资产与侧栏（待用户手动验收）

- [ ] 首次加载确认前置字幕默认开启且范围为 20 秒。
- [ ] 确认顶部缩放按钮位于中文 / EN 左侧且同排同高，顺序为 `+ / −`；从默认 100% 逐档调整，确认每次变化 10%、75% 与 200% 达到边界时对应按钮禁用、关闭重开恢复上次比例。确认双指捏合和 `Ctrl/Command + 滚轮`不会缩放，普通滚动保持正常；分别检查中文、英文和窄侧栏布局。
- [ ] 在 YouTube 和哔哩哔哩分别开启播放器字幕，播放超过 20 秒后创建标记，确认卡片显示标记前字幕；连续扩展的滚动字幕不会重复，句号、问号或叹号结束完整段落，窗口首尾残句仍保留。
- [ ] 在 YouTube 完整字幕已加载后分别创建文字笔记、侧栏语音笔记和快捷键语音笔记，确认三条入口都优先保存当前范围内的本地完整段落；完整译文存在时卡片与 Markdown 分开显示“原文 / 译文”，翻译只有部分完成时不保存译文。重试字幕、切换视频或在 Bilibili 创建笔记，确认旧本地字幕不会串入且继续回退到播放器画面字幕。
- [ ] 依次选择 5、10、20、30 秒并创建标记，确认新标记使用对应范围。
- [ ] 编辑字幕后失焦保存，刷新侧栏并导出 ZIP，确认侧栏和 Markdown 保留修改。
- [ ] 按 `Esc` 取消一次字幕编辑，确认原内容保持不变。
- [ ] 关闭前置字幕后创建标记，确认停止采集且所有字幕块隐藏；重新开启后历史字幕重新出现。
- [ ] 播放器未开启字幕时创建标记，确认显示空字幕提示，并可手工输入字幕。
- [ ] 在有原生字幕的 YouTube 视频中打开“完整字幕”，确认标题始终保持单行，状态显示段数、覆盖起止时间和字幕语言；“合并条数”、5/10/20 三选一、紧凑的“翻译 / 定位 / 重试”以及字号 +/− 按钮都保持在同一行并对齐。默认选中 5；每段从第 5 条开始延伸到下一处句号、问号或叹号，最多到第 10 条，无强标点时优先在逗号、分号、冒号或顿号处切分，完全无标点时按 10 条切分；10/20 档位按相同两倍规则工作。播放到首段前、字幕段内、段落间隔和末段后分别点击“定位”，确认字幕列表居中滚动到相应段落并短暂高亮，视频进度和播放状态不变；点击时间范围仍跳到该段第一条字幕。
- [ ] 点击完整字幕“翻译”，确认请求按断句后的完整段落执行，进度总数对应当前段落数，翻译期间 5/10/20 档位禁用且不显示查看选项；全部完成后，“翻译”和“重试”之间出现紧凑的“原文 / 译文”勾选框。分别验证仅原文、仅译文和双语模式，确认不能同时取消两项，关闭重开后恢复上次选择。切换到未完整翻译的档位时选项隐藏并显示原文，切回已缓存完整译文的档位后选项和偏好恢复。确认进度从个位进入十位、百位后，斜杠、总数和周边状态文字不左右抖动。
- [ ] 在 Edge 150、Edge 152 和 Chrome 138+ 分别确认字幕源语言自动识别，“翻译为”只提供五种目标语言，且没有翻译方式、API Base URL、API Key 或模型名配置。
- [ ] 在三组浏览器环境分别执行英→简中首次翻译，确认先检测 `zh-Hans`、不支持时回退 `zh`；再选择日语或韩语目标，确认使用新语言对。各语言对均显示下载进度，完成后显示“可断网翻译”和预估占用，原文与译文上下对照。
- [ ] 各浏览器下载完成后断网并重新翻译，确认字幕翻译继续成功，开发者工具中没有字幕内容网络请求；Edge 与 Chrome 的语言包状态互不假定为共享。
- [ ] 验证源语言与所选目标语言相同时提示无需翻译；对 Edge 不支持的语言对和不支持 Translator API 的浏览器显示明确状态并禁用翻译，不向外部字幕翻译服务发送内容。
- [ ] 下载语言包时模拟失败，确认显示下载失败状态且可重试；翻译中断后保留已完成译文并可“继续翻译”。
- [ ] 翻译进行中依次重试字幕、切换视频、关闭侧栏，确认任务中断且 Translator 会话销毁；重新打开或切换回来后没有跨视频译文。
- [ ] 从 1.0.17 升级后确认旧版翻译服务地址、密钥、模型和主机记录已从扩展本机存储清除，截图授权不受影响。
- [ ] 在侧栏点击“启用播放器截图”并完成 Edge 可选权限授权，确认文字笔记和语音笔记都能保存当前可见播放器截图。
- [ ] 在独立扩展授权页完成麦克风授权后，按住右 Option/Alt 录制一条语音笔记；确认松开后保存原始录音并恢复视频播放。
- [ ] 创建一条文字笔记和一条语音笔记，确认侧栏均显示截图。
- [ ] 确认语音笔记可以播放保存的原始录音；点击缩略图可放大并关闭。
- [ ] 分别在 YouTube 和哔哩哔哩播放视频后点击快速标记输入框，确认视频暂停；输入正文后单击已暂停的播放器一次，确认笔记自动保存且视频立即续播，无需第二次点击。
- [ ] 保持受支持的视频页打开，重新加载扩展后不刷新视频页，直接打开侧栏；确认标题与快速标记输入框恢复，点击输入框可暂停视频并保存或取消标记。
- [ ] 保持侧栏打开，在 Edge 扩展菜单中关闭再开启当前视频网站的扩展总开关并按提示刷新；确认侧栏先显示中文权限指引，随后自动恢复标题与快速标记输入框，无需关闭侧栏。
- [ ] 依次切换课程 A、普通网页、课程 B、课程 A，确认侧栏依次显示 A、隐藏、显示 B、恢复 A。
- [ ] 确认侧栏只在 YouTube 普通视频页和哔哩哔哩普通 BV 视频页启用。
- [ ] 与网课声伴一起使用时，确认语音期间 A/B 都暂停，松开后按恢复资格恢复；文字输入保留现有 A/B 接力行为。
- [ ] 网课声伴处于“全部暂停”时结束语音，确认 A/B 不恢复；关闭或切换普通标签后确认视频与录音租约释放。

## 四、导出与异常路径（待用户手动验收）

- [ ] 导出 ZIP，检查 Markdown、`images/`、`audio/`、时点链接和相对路径。
- [ ] 模拟前置字幕开关和读取范围写入失败，确认控件恢复上一有效值并显示错误提示。
- [ ] 模拟字幕保存失败，确认未保存文本保持可见；再次点击“编辑字幕”后确认原输入仍在，并可重试保存成功。
- [ ] 在侧栏刷新请求尚未返回时开始编辑正文或字幕，确认迟到响应不会替换编辑区；保存或取消后确认时间线只刷新一次。
- [ ] 拒绝麦克风权限后，确认视频与录音租约均已释放。
- [ ] 断网后开始模型下载或转写，确认错误可见且视频与录音租约均已释放。
- [ ] 录音、下载或转写期间关闭课程标签，确认视频与录音租约均已释放。

## 五、Chrome 兼容验收（待用户手动验收）

- [ ] 在桌面 Chrome 138 或更高版本通过 `chrome://extensions/` 加载同一份 `dist`，确认 Manifest、权限和侧栏正常启用。
- [ ] 在 YouTube 与哔哩哔哩分别验证侧栏上下文、字幕读取、文字笔记、截图、跳转、撤销/反撤销和清空。
- [ ] 完成本地 Whisper 模型下载、录音、转写和重新转写，确认 Chrome 下的 offscreen、Worker、WASM 与麦克风授权路径正常。
- [ ] 导出包含 Markdown、截图和原始录音的 ZIP，确认内容与 Edge 一致且没有额外权限提示。

## 六、笔记历史与删除（待用户手动验收）

- [ ] 创建文字笔记和语音笔记后，分别执行“撤销”和“反撤销”，确认时间点新增可撤销；新建视频会话不会被撤销移除。
- [ ] 编辑个人正文和前置字幕后，确认可通过 `Cmd/Ctrl + Z` 撤销，并通过 `Cmd/Ctrl + Shift + Z` 重做；执行新的编辑后确认重做失效。
- [ ] 连续执行超过 50 次可撤销操作，重启 Edge 后确认仅保留最近 50 次持久历史，并可继续撤销或重做。
- [ ] 删除单条笔记时确认出现确认框；取消后笔记仍在，确认后笔记从时间线隐藏并可撤销恢复。
- [ ] 点击“清空”时确认出现确认框；取消后会话不变，确认后全部笔记以一次原子操作清空并可一次撤销恢复。
- [ ] 删除包含截图和录音的笔记后撤销，确认资产仍可显示和播放；当历史不再引用该软删除笔记后，确认截图和录音资产被最终回收。

## 七、数据清理（待用户手动验收）

- [ ] 确认已下载模型没有删除入口，导出不会删除本地会话或模型。
- [ ] 在 Edge 扩展管理页清除本扩展数据，确认 Base、Small、Medium 缓存和本地笔记统一释放。
